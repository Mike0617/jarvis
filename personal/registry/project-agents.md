# Project Agents 管理表

本檔案僅作為專案註冊表，完整規範請參考 `personal/CORE.md`。

## 專案清單
- **caster-web**
  - 路徑：`${CASTER_WEB_PATH}`
  - 角色：前端專案代理
  - 特例：路由必須 `/v2/` 前綴

- **s8_agent**
  - 路徑：`${S8_AGENT_PATH}`
  - 角色：後端專案代理（MVC + API 並存）
  - 特例：API 路由必須 `/api/` 前綴
